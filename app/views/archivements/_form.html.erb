<%= form_for(@archivement) do |f| %>
  <% if @archivement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@archivement.errors.count, "error") %> prohibited this archivement from being saved:</h2>

      <ul>
      <% @archivement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_area :desc %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.select :category_id, Category.all.map {|c| [c.name, c.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
